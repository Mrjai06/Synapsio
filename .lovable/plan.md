

## Increase Logo Size by 200%

The selected logo image in `ProgressNavbar.tsx` currently has a width of `w-[300px]`. Increasing by 200% means tripling the current size.

### Change
- **File**: `src/components/ProgressNavbar.tsx`, line 96
- Update the logo width from `w-[300px]` to `w-[900px]`
- Position and centering remain unchanged (the `w-[9px]` wrapper with `justify-center` keeps it aligned to the dot column)

